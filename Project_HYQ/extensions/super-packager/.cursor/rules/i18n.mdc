---
description: 国际化 (i18n) 使用规范和多语言支持
---

# Cocos Creator 扩展国际化规范

## 语言文件结构

### 文件位置和格式
- **中文**: [i18n/zh.js](mdc:i18n/zh.js) - 默认导出对象
- **英文**: [i18n/en.js](mdc:i18n/en.js) - 默认导出对象

```javascript
// zh.js
export default {
  'super-packager': {
    title: '超级打包器',
    description: '强大的资源打包工具'
  },
  menu: {
    panel: {
      'super-packager': '超级打包器'
    }
  }
}
```

### 支持的语言代码
- `zh` - 简体中文
- `en` - 英文
- 其他标准 ISO 639-1 语言代码

## package.json 中的国际化

### 基本语法
使用 `i18n:` 前缀引用国际化键：

```json
{
  "title": "i18n:super-packager.title",
  "description": "i18n:super-packager.description",
  "contributions": {
    "menu": [
      {
        "path": "i18n:menu.panel/super-packager",
        "label": "i18n:super-packager.title",
        "message": "open"
      }
    ]
  }
}
```

### 菜单路径国际化
- `path` 字段支持国际化，影响菜单层级显示
- `label` 字段支持国际化，影响菜单项文本

## 多语言键值规范

### 命名约定
- 使用英文小写，单词间用连字符 `-` 分隔
- 层级结构用点号 `.` 分隔
- 避免过深的嵌套（建议不超过3层）

### 推荐结构
```javascript
export default {
  // 扩展基本信息
  'extension-name': {
    title: '扩展标题',
    description: '扩展描述'
  },
  
  // 菜单相关
  menu: {
    panel: '面板',
    tools: '工具',
    help: '帮助'
  },
  
  // UI 组件文本
  ui: {
    buttons: {
      ok: '确定',
      cancel: '取消',
      apply: '应用'
    },
    messages: {
      success: '操作成功',
      error: '操作失败'
    }
  },
  
  // 错误消息
  errors: {
    'file-not-found': '文件未找到',
    'permission-denied': '权限不足'
  }
}
```

## 编辑器 i18n API

### 在扩展代码中使用
```typescript
// Browser 端
const text = Editor.I18n.t('extension-name.ui.buttons.ok');

// 带参数的翻译
const message = Editor.I18n.t('extension-name.errors.file-error', { filename: 'test.txt' });
```

### 在 Vue 组件中使用
```vue
<template>
  <el-button>{{ t('ui.buttons.ok') }}</el-button>
</template>

<script setup lang="ts">
// 创建翻译函数
const t = (key: string) => Editor.I18n.t(`super-packager.${key}`);
</script>
```

## 最佳实践

### 文本管理
1. **完整性**: 确保所有语言文件包含相同的键
2. **一致性**: 相同概念在不同位置使用相同的翻译
3. **简洁性**: 避免过长的文本，考虑 UI 布局限制

### 开发流程
1. 先用英文键名占位：`'save-file'`
2. 在所有支持的语言文件中添加翻译
3. 测试不同语言环境下的 UI 显示效果

### 参数化翻译
```javascript
// zh.js
export default {
  messages: {
    'process-complete': '处理完成，共处理 {count} 个文件'
  }
}

// 使用
const message = Editor.I18n.t('extension-name.messages.process-complete', { count: 10 });
```

## 常见问题

### 翻译未生效
1. 检查键名是否正确，包括大小写和层级
2. 确认语言文件导出格式为默认导出
3. 验证 `i18n:` 前缀使用是否正确

### 菜单显示问题
- 菜单路径国际化可能导致菜单位置变化
- 建议菜单路径使用稳定的英文结构
- 只对菜单标签进行国际化

### 动态语言切换
- 编辑器重启后生效
- 面板需要重新打开以显示新语言

## 扩展语言支持

### 添加新语言
1. 在 `i18n/` 目录创建对应语言文件（如 `ja.js`）
2. 复制现有语言文件结构
3. 翻译所有文本内容
4. 测试新语言环境

### 语言文件维护
- 定期检查缺失的翻译键
- 保持各语言文件的同步更新
- 使用工具验证翻译完整性